# 全面审计报告 — 工作区未提交修改（2026-08-09）

- **范围**：相对 HEAD 的全部未提交改动（33 文件，~3150 行），含
  - 本轮新功能：配置文件支持（`src/config.rs`）、GitHub Actions release（`.github/workflows/release.yml`）、安装脚本（`release/install.sh`）、MIT LICENSE、示例配置
  - 遗留未提交的 Phase 7 工作：`src/agent.rs`、`src/session/mod.rs`、`src/tools/scratchpad.rs`、`src/compaction.rs`、`src/plugin/`、相关测试
- **方法**：隔离代码审查（review）+ 安全审查（security_review）+ 手动逐项核实与端到端复验
- **结论**：初始 verdict=block（3 高危 + 4 中危 + 若干低危）；**全部高危与中危已修复并复验**；遗留中危（M1/M2/M3）列入建议。

---

## 一、高危（Blocking）— 已全部修复 ✅

| # | 位置 | 问题 | 修复 |
|---|------|------|------|
| H1 | `release.yml` 打包/上传 | artifact 生成在 job 根目录，上传 path 却指向 `target/<target>/release/`（无包）→ 5 平台上传为空、publish 必失败 | 上传 path 改为 `${{ matrix.artifact }}`，加 `if-no-files-found: error` 显式失败 |
| H2 | `src/compaction.rs:109` | `&output[..80]` 字节切片，中文/emoji 工具输出落在字符中间即 panic（长任务必触发） | 用 `char_indices().nth(80)` 找安全边界；新增多字节边界测试（`test_multibyte_tool_output_no_panic`） |
| H3 | `release.yml` Windows 分支 | Git Bash 的 GNU tar 不支持 zip 容器，`tar -a` 产 zip 不可靠 | Windows 分支改用原生 PowerShell `Compress-Archive`；Unix 分支保留 `tar -a` |

## 二、安全（Security Review）— 高危/中危已修复 ✅

| # | 位置 | 问题 | 修复 |
|---|------|------|------|
| S1 | `release/install.sh` | curl 无 `--` 分隔（URL 以 `-` 开头可被当选项）；下载物无校验 | curl 改为 `-o file -- URL`（选项在前、`--` 紧贴 URL）；新增 `AURA_SHA256` 强制校验 |
| S2 | `release.yml` publish | `workflow_dispatch` 的 tag 直接插入脚本，可命令注入 | 新增 Validate tag 步骤：`^v[0-9]+\.[0-9]+\.[0-9]+$` 才放行；tag 经 env 传入 |
| S3 | `release/install.sh` zip 分支 | `unzip -d` 全量解压（zip-slip/解压炸弹面） | 改为 `unzip -j ... "aura.exe"` 只提取单条目 |

> 安全审查明确无误项：`plugin/secret.rs` 的 `unsafe env set_var` 仅存在于 `#[cfg(test)]`；`main.rs` 警告与 `model_http.rs` 错误路径均不打印 key；MCP env 覆盖防护正确。

## 三、中危（Should-fix）— M4 已修复，M1/M2/M3 建议

| # | 位置 | 问题 | 状态 |
|---|------|------|------|
| M4 | `src/session/mod.rs:push` | 先推内存后写 transcript；append 失败时两者不同步 | ✅ 已修复：先 `transcript.append` 成功再 `messages.push` |
| M1 | `src/agent.rs` + `compaction.rs` | 压缩后系统消息永久丢失（第二次压缩只留占位符）；超阈值后每轮重复 O(n) 压缩 | ⚠️ 建议：System 消息排除出 early；压缩结果写回 session（agent.rs:142 注释已预告方向） |
| M2 | `src/tools/scratchpad.rs` | 主 agent 与 subagent 各持内存副本，persist 全量覆盖 → 并发丢失更新 | ⚠️ 建议：persist 前重读合并 / 文件锁 / append-only JSONL |
| M3 | `src/main.rs:resume` | resume 会话元数据 model 与 `choose_model` 实际生效值不一致 | ⚠️ 建议：先算合并后 model 再传给 Session |

## 四、低危（Nits）— 记录

- `HttpConfig` `#[derive(Debug)]` 含明文 api_key（当前无 `{:?}` 输出点，未来 debug 日志需打码）
- `model_http.rs` 非 2xx 时原样回显 provider body（建议截断）
- `config.toml` 建议权限 0600（文档提示即可）
- `--api-key` 经 CLI 传入会暴露于 `ps`（文档已推荐 env）
- `agent.rs:146` 非压缩分支每轮 `to_vec()` 全量克隆（性能）
- `compaction.rs` 80% 阈值 `max * 80 / 100` 理论溢出（值域可忽略）
- `release.yml` `dtolnay/rust-toolchain@master` 未 pin；quality/build job 未声明最小 permissions
- `gh release create` 对已存在 tag 会失败（重复发布场景）
- `scratchpad.rs` 损坏 JSON 静默置空后覆盖原文件（数据丢失面）
- `--fake-model` 也执行 `Config::load()`，配置损坏时 fake 模式被阻断（设计取舍，测试已固化）

## 五、审计修复过程中新发现并修复的问题

| # | 位置 | 问题 | 修复 |
|---|------|------|------|
| H4 | `release/install.sh` curl | 安全修复时 `--` 位置放反：`curl -- URL -o file` 使 `-o` 失效 → 二进制写到 stdout、`-o`/路径被当 URL | 改为 `curl -o file -- URL` |
| H5 | `release/install.sh` SHA256 | `ARTIFACT##*.` 只取最后扩展名（`gz`），校验路径 `pkg.gz` 不存在 | 改用 `ARTIFACT#*.`（→ `tar.gz`/`zip`） |
| H6 | `release/install.sh` CHECKER | macOS `/sbin/sha256sum` 不支持 `-c`，`command -v` 却能找到 | 优先 `shasum -a 256 -c`（macOS/Linux 均支持），无 shasum 才退回 `sha256sum -c` |

## 六、复验结果（修复后）

| 项目 | 结果 |
|---|---|
| `cargo test --workspace` | ✅ 356 tests（含新增 compaction 多字节边界测试） |
| `cargo fmt --check` / clippy `-D warnings` | ✅ / ✅ 0 警告 |
| release.yml YAML + 上传路径一致性 | ✅ upload path = `${{ matrix.artifact }}` + `if-no-files-found: error` |
| install.sh 端到端（本地 HTTP 模拟 release） | ✅ macos-arm64 下载→解压→安装→`aura 0.1.0`，exit 0 |
| install.sh AURA_SHA256 正确值 | ✅ 安装成功，exit 0 |
| install.sh AURA_SHA256 错误值 | ✅ 拒绝安装，exit 1 |
| install.sh bash -n | ✅ |

## 七、测试覆盖缺口（后续）

- compaction：已补多字节边界；压缩循环语义（M1）无集成测试
- scratchpad：无并发丢失更新 / 损坏文件场景测试
- release.yml：CI 首次运行才能真正验证（本地只能模拟打包与安装）

## 八、建议的后续动作

1. M1（压缩写回 session）是长任务正确性的关键，建议下一 loop 优先
2. M2（scratchpad 并发）在 subagent 场景会丢数据，建议下一 loop 处理
3. `permissions: contents: read` 补到 quality/build job；`rust-toolchain` 改 pin 版本
4. 推送后观察 Actions 首跑（Linux ARM64 runner / Windows zip / publish 链路）
