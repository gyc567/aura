# Loop 约束

> 使用 `/constraints <规则>` 添加规则。
> `loop-constraints` skill 在每次运行开始时读取此文件。
> 此处的约束是**强制性的** — Agent 必须遵守。

## 推送与合并
- 推送前必须告知我
- 未经人工批准不得自动合并到 main
- 始终先创建 draft PR；让我审核后再标记 ready

## 路径
- 禁止编辑 .env、.env.*、auth/、payments/、secrets/、credentials/
- 未经人工批准禁止编辑基础设施配置

## 代码
- 提交修复前必须运行测试
- 不得通过禁用测试来让 CI 变绿
- 不得重构无关代码 — 每次运行只修一个问题
- 每个问题最多尝试 3 次；超限后升级处理
- 机械执行尝试限制：每次尝试写入 `loop-ledger.json`，重试前运行 `loop-context --check`（见 `loop-guard` skill）

## 沟通
- 做事前必须告知我
- 未经我批准不得关闭 issue 或 PR

## 预算
- Token 消耗达到日额度 95% 时，切换为仅报告模式（2026-08-07 从 80% 提高；实际使用 5h 内 ~39% 远低于限额）
- 如果 loop-pause-all 生效，立即退出

---
<!-- 你可以在下方添加自己的规则。使用英文。此文件会被逐字读取。-->
